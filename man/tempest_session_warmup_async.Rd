% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/costorm-warmup.R
\name{tempest_session_warmup_async}
\alias{tempest_session_warmup_async}
\title{Warm up a Co-STORM session asynchronously}
\usage{
tempest_session_warmup_async(
  session,
  timeout_s = getOption("tempest.costorm.warmup_timeout_s", 120),
  max_parallel_experts = getOption("tempest.costorm.warmup_max_parallel_experts", 3L),
  is_current = function() TRUE
)
}
\arguments{
\item{session}{A \link{TempestSession} created by \code{\link[=tempest_session]{tempest_session()}}.}

\item{timeout_s}{Maximum seconds allowed for each expert orientation. Use
\code{NULL}, a non-positive value, or a non-finite value to disable timeouts.}

\item{max_parallel_experts}{Maximum number of expert requests started at the
same time.}

\item{is_current}{A zero-argument function that returns \code{TRUE} while the
originating host session is current. Stale operations resolve without
committing late results.}
}
\value{
A promise that resolves to an internal \code{tempest_warmup_result} S7
object with aggregate counts and per-expert audit records.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Runs one bounded, evidence-seeking orientation for each active expert without
blocking the caller. Expert requests run in bounded parallel batches, while
transcript and evidence commits occur deterministically in expert order. A
single mind-map update follows the completed evidence commits.
}
\keyword{internal}
