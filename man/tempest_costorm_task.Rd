% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evals.R
\name{tempest_costorm_task}
\alias{tempest_costorm_task}
\title{Create a Co-STORM evaluation task using SimulatedUser}
\usage{
tempest_costorm_task(
  dataset = c("qa"),
  config = tempest_config(),
  max_turns = 5L,
  solver = NULL,
  scorer = NULL,
  scorer_chat = NULL,
  ...
)
}
\arguments{
\item{dataset}{Which built-in dataset to use. Currently "qa".}

\item{config}{A \code{TempestConfig}.}

\item{max_turns}{Maximum turns per simulated session.}

\item{solver}{Optional vitals-compatible solver. When \code{NULL}, uses the
built-in simulated Co-STORM session solver.}

\item{scorer}{Optional vitals-compatible scorer. When \code{NULL}, uses
\code{vitals::model_graded_qa()}.}

\item{scorer_chat}{Optional chat for the default model-graded scorer. When
\code{NULL}, a judge chat is created from \code{config}.}

\item{...}{Passed to \code{vitals::Task$new()}.}
}
\value{
A \code{vitals::Task}.
}
\description{
Runs automated Co-STORM sessions with a simulated user for evaluation.
Moderator and expert turns use the same persistent Deputy agents as normal
Co-STORM sessions. The built-in solver completes a real \code{\link[=tempest_session]{tempest_session()}},
reads its exact committed report, and includes credential-safe product
summaries and terminal Deputy traces in \code{solver_metadata}; it never returns
Deputy Agent objects, chats, clients, tools, or credentials as metadata.
}
\examples{
\dontrun{
task <- tempest_costorm_task(config = tempest_config(), max_turns = 5)
task$eval()
}
}
