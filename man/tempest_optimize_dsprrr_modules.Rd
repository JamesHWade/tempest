% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storm.R
\name{tempest_optimize_dsprrr_modules}
\alias{tempest_optimize_dsprrr_modules}
\title{Optimize STORM dsprrr modules}
\usage{
tempest_optimize_dsprrr_modules(
  trainsets,
  modules = NULL,
  config = tempest_config(),
  teleprompter = NULL,
  valsets = NULL,
  .llm = NULL,
  save_dir = NULL,
  k = 4L,
  seed = 123L,
  strict = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{trainsets}{Named list of training data frames. Names must match module
names from \code{tempest_make_dsprrr_modules()}, such as
\code{"query_decomposition"}, \code{"fact_extraction"}, \code{"section_writing"}, or
\code{"lead_section"}.}

\item{modules}{Optional named list of modules to optimize. Defaults to fresh
modules from \code{tempest_make_dsprrr_modules(config)}.}

\item{config}{A \code{TempestConfig} object used when \code{modules} is \code{NULL}.}

\item{teleprompter}{Optional dsprrr teleprompter, named list of
teleprompters, or factory function. Defaults to
\code{dsprrr::LabeledFewShot()}.}

\item{valsets}{Optional named list of validation data frames.}

\item{.llm}{Optional ellmer chat object passed to dsprrr compilation.}

\item{save_dir}{Optional directory or \code{.rds} path for the optimized module
list.}

\item{k}{Number of examples for the default \code{LabeledFewShot} teleprompter.}

\item{seed}{Random seed for the default teleprompter.}

\item{strict}{If \code{TRUE}, abort on the first compile failure. If \code{FALSE},
warn and keep the unoptimized module for that step.}

\item{verbose}{If \code{TRUE}, print optimization progress.}
}
\value{
A named list of dsprrr modules.
}
\description{
Compiles selected STORM dsprrr modules against user-provided training data.
This is an explicit optimization step: run it with labeled examples, then
pass the returned module list to \code{\link[=tempest_run]{tempest_run()}} via \code{dsprrr_modules}.
}
\examples{
\dontrun{
trainset <- data.frame(
  question = "What are battery recycling bottlenecks?",
  topic = "lithium batteries"
)
trainset$queries <- list(c("battery recycling", "EV battery capacity"))
modules <- tempest_optimize_dsprrr_modules(
  trainsets = list(query_decomposition = trainset),
  config = tempest_config()
)
}
}
