% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{tempest_config}
\alias{tempest_config}
\title{Create a STORM configuration}
\usage{
tempest_config(
  models = NULL,
  params = NULL,
  chat_fn = NULL,
  embed_fn = NULL,
  ragnar_store = NULL,
  artifact_store = NULL,
  search_provider = "native",
  cache_dir = NULL,
  cache_enabled = TRUE,
  cache_ttl = Inf,
  max_search_results = 8,
  max_search_queries_per_turn = 3,
  retrieve_top_k = 25,
  max_sources = 24,
  user_agent = "tempest (R; +https://github.com/JamesHWade/tempest)",
  node_expansion_trigger_count = NULL,
  enable_discourse_manager = FALSE,
  max_active_experts = 5L,
  enable_unseen_surfacing = FALSE,
  citation_policy = "source_attributed",
  min_support_score = 0.7,
  on_unsupported_claim = "flag"
)
}
\arguments{
\item{models}{Named list of model identifiers for each role, or a single
string to use for all roles.}

\item{params}{Additional parameters passed to chat creation. Explicit values
override the role defaults used by built-in ChatGPT-subscription clients.}

\item{chat_fn}{Custom chat factory function. Should accept \code{role}, \code{model},
\code{system_prompt}, and \code{echo} arguments and return an ellmer-compatible Chat
object. Use this for custom providers like \code{chat_company()}.}

\item{embed_fn}{Embedding function for RAG. Should accept a character vector
and return a matrix of embeddings. Use \code{ragnar::embed_openai()},
\code{ragnar::embed_ollama()}, or a custom function. If provided, a ragnar store
is automatically created.}

\item{ragnar_store}{A pre-built ragnar store. If NULL and \code{embed_fn} is
provided, a store is created automatically with the tempest metadata schema.}

\item{artifact_store}{Optional artifact-store adapter from
\code{tempest_artifact_store()} or \code{tempest_memory_artifact_store()}.}

\item{search_provider}{Search provider: "native" (use provider's built-in web
search when available), "wikipedia", "you", "bing", "serper", "brave",
"duckduckgo", "tavily", "searxng", "google", or "azure_ai_search". Default
is "native" which uses OpenAI, Anthropic, or Google's native web search
capabilities when available, falling back to "wikipedia".}

\item{cache_dir}{Path to cache directory.}

\item{cache_enabled}{Whether retriever search/fetch calls should read from
and write to the on-disk cache.}

\item{cache_ttl}{Maximum cache age in seconds. Defaults to \code{Inf}, which keeps
cached search/fetch entries valid until explicitly cleared.}

\item{max_search_results}{Maximum search results to return.}

\item{max_search_queries_per_turn}{Maximum decomposed queries per research
turn.}

\item{retrieve_top_k}{Maximum facts/chunks retrieved for each section.}

\item{max_sources}{Maximum sources to keep.}

\item{user_agent}{User agent string for HTTP requests.}

\item{node_expansion_trigger_count}{Number of notes/sources per node that
triggers expansion (NULL = disabled).}

\item{enable_discourse_manager}{Whether to enable LLM-driven discourse
management in Co-STORM.}

\item{max_active_experts}{Maximum number of active expert agents in Co-STORM.}

\item{enable_unseen_surfacing}{Whether to surface undiscussed sources in
Co-STORM.}

\item{citation_policy}{Citation enforcement policy: one of "none",
"source_attributed", "claim_verified", or "strict".}

\item{min_support_score}{Minimum support score in \verb{[0, 1]} for a claim to be
considered supported.}

\item{on_unsupported_claim}{Action for unsupported claims: one of "flag",
"drop", "revise", or "keep_with_warning". \code{drop} removes the complete
unsupported assertion; \code{revise} replaces it with a revision notice.}
}
\value{
A \code{TempestConfig} S7 object.
}
\description{
Create a STORM configuration
}
\section{Default chat}{

When both \code{models} and \code{chat_fn} are \code{NULL}, \code{tempest_config()} consults the
\code{tempest.chat} R option. Set it to a provider/model string accepted by
\code{\link[ellmer:chat]{ellmer::chat()}} or an ellmer \code{Chat} object. String values are used for every
role. Chat objects are cloned for every role, retain their provider settings
and system instructions, and receive the appropriate Tempest role prompt.
When the option is unset, Tempest creates its built-in OpenAI clients with
\code{\link[ellmer:chat_openai]{ellmer::chat_openai()}} and \code{auth = "codex"}, which uses file-backed ChatGPT
subscription authentication managed by Codex CLI. Explicit \code{models} and
\code{chat_fn} arguments take precedence over the option.
}

\examples{
cfg <- tempest_config()
cfg <- tempest_config(search_provider = "wikipedia")
}
