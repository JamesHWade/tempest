% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress-events.R
\name{tempest_progress_collector}
\alias{tempest_progress_collector}
\title{Create an in-memory Tempest progress event collector}
\usage{
tempest_progress_collector(include_payload = FALSE)
}
\arguments{
\item{include_payload}{If \code{TRUE}, retain event payloads. The default \code{FALSE}
stores event metadata only.}
}
\value{
A \code{tempest_progress_collector} list with \code{record()}, \code{events()},
\code{data()}, \code{replay()}, and \code{clear()} methods.
}
\description{
\code{tempest_progress_collector()} returns a small host-neutral sink for Tempest
progress events. Use \code{collector$record} as a \code{progress} callback, then read
events back with filtering or replay them to another callback.
}
\details{
By default, the collector strips event payloads before storing them. Set
\code{include_payload = TRUE} when tests or trusted host adapters need to assert
or replay structured payload metadata.
}
\examples{
collector <- tempest_progress_collector()
collector$record(tempest_progress_event(
  run_id = "run-1",
  workflow = "storm",
  event_type = "stage",
  status = "started",
  stage = "research"
))
collector$data(stage = "research")
}
