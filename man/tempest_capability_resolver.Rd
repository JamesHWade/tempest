% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{tempest_capability_resolver}
\alias{tempest_capability_resolver}
\title{Create a Tempest capability resolver}
\usage{
tempest_capability_resolver(
  specifications = list(),
  implementations = list(),
  connection_provider = NULL
)
}
\arguments{
\item{specifications}{Optional list of \code{\link[=tempest_capability_spec]{tempest_capability_spec()}} objects.}

\item{implementations}{Named runtime factories or descriptor lists with
\code{factory} and optional \code{authorize} functions. A factory receives
\code{capability_spec}, named \code{connections}, and runtime \code{context}, and returns
a list containing \code{tools}, \code{registrars}, and optional serializable
\code{metadata}.}

\item{connection_provider}{Optional \code{\link[=tempest_connection_provider]{tempest_connection_provider()}} used to
resolve explicitly allowed opaque connection references.}
}
\value{
A mutable resolver with registration, inspection, and \code{resolve()}
methods. Resolution returns runtime tools and registrars plus serializable
grant records.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
The resolver keeps serializable capability specifications separate from
runtime factories. It preflights every requested capability before invoking
factories, treats required failures as errors, and records optional failures
as serializable denied grants.
}
\examples{
specification <- tempest_capability_spec(
  "documents.search",
  purpose = "Search approved documents",
  instructions = "Search only the granted index.",
  operation_id = "capability.documents.search"
)
resolver <- tempest_capability_resolver(
  list(specification),
  implementations = list(
    "documents.search" = function(capability_spec, connections, context) {
      list(tools = list(), registrars = list())
    }
  )
)
resolver$resolve(required_capability_ids = "documents.search")
}
