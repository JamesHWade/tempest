% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{tempest_resource}
\alias{tempest_resource}
\title{Create a typed evidence resource}
\usage{
tempest_resource(
  resource_kind,
  locator,
  title,
  media_type,
  resource_id = NULL,
  content = NULL,
  storage_ref = NULL,
  origin_connection_id = NULL,
  scope_metadata = list(),
  content_hash = NULL,
  retrieved_at = NULL,
  redaction = list(),
  retention = list(),
  metadata = list()
)
}
\arguments{
\item{resource_kind}{Stable resource-kind identifier such as \code{"web"},
\code{"file"}, or \code{"scientific.document"}.}

\item{locator}{Opaque locator or URI. Tempest records it but does not resolve
it outside a resource-kind adapter.}

\item{title}{Display title.}

\item{media_type}{IANA media type.}

\item{resource_id}{Optional stable resource identifier. By default it is
derived from \code{resource_kind} and \code{locator}.}

\item{content}{Optional inline canonical content.}

\item{storage_ref}{Optional opaque reference to externally stored content.}

\item{origin_connection_id}{Optional host connection reference identifier.}

\item{scope_metadata}{Serializable tenant or project scope metadata.
Credential-like field names and values are rejected recursively.}

\item{content_hash}{Optional content checksum. Tempest computes one for
inline content when omitted.}

\item{retrieved_at}{Retrieval timestamp.}

\item{redaction}{Serializable redaction metadata. Credential-like field names
and values are rejected recursively.}

\item{retention}{Serializable retention metadata. Credential-like field names
and values are rejected recursively.}

\item{metadata}{Serializable namespaced host metadata. Credential-like field
names and values are rejected recursively.}
}
\value{
A \code{tempest_resource} S7 object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Resources identify provisional scientific evidence without requiring a
public URL. The durable value may describe a web page, file, lab record, or
database result used during research. Authenticated clients and credentials
remain host-owned and are never stored here. This record is not a generic
connection-management contract; its 0.2 role narrows to scientific source
and context evidence in a research workspace.
}
\keyword{internal}
