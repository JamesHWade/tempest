% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny-adapter.R
\name{tempest_shiny_ui}
\alias{tempest_shiny_ui}
\title{Embed Tempest panels in a Shiny UI}
\usage{
tempest_shiny_ui(
  id,
  panels = c("chat", "sources", "facts", "mindmap", "transcript", "report"),
  show_config = FALSE
)
}
\arguments{
\item{id}{Shiny module id.}

\item{panels}{Character vector of panels to include. Use \code{"all"} for every
panel. The default embeds the Co-STORM chat and durable research views.}

\item{show_config}{If \code{TRUE}, include the bundled configuration controls in
the Chat sidebar. Hosts that provide their own config should leave this as
\code{FALSE}.}
}
\value{
A Shiny tag object.
}
\description{
\code{tempest_shiny_ui()} is a compact Shiny module UI for host applications that
want to embed Tempest without sourcing files from \code{inst/shiny/R}. It reuses
the bundled app's panels while letting the host provide the page shell,
configuration, storage policy, and surrounding controls.
}
\examples{
\dontrun{
ui <- bslib::page_fillable(tempest_shiny_ui("research"))
}
}
