% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knowledge-artifacts.R
\name{tempest_artifact_knowledge}
\alias{tempest_artifact_knowledge}
\title{Bring a retained artifact selection into a research run}
\usage{
tempest_artifact_knowledge(selection, contents)
}
\arguments{
\item{selection}{A JSON-compatible list with \code{selection_id} (host-owned
identity), \code{purpose}, \code{records}, and \code{provenance} (a list of source references).
Each record has \code{record_id}, \code{revision_id}, \code{class}, \code{sha256} (lowercase
SHA-256 of UTF-8 text bytes), and \code{dependencies} (a list of exact
\code{record_id}/\code{revision_id} pairs). Supported classes are \code{Claim},
\code{ClaimSupport}, \code{EvidenceSpan}, and \code{Source}. The selection contains 1 to
1000 unique records; its metadata is limited to 1 MiB. Record and revision
identifiers must not contain surrounding whitespace. Dependencies describe
the host's evidence selection, not an inferred graph or authorization.}

\item{contents}{Named list of retained text strings, keyed by \code{record_id},
with exactly the selection's records and at most 1 MiB of UTF-8 text total.
Claims may contain a JSON object with \code{statement_text} and \code{status} fields,
preserving multiline statements exactly. Plain text claims use inert
\code{statement_text: ...} and \code{status: ...} lines. Active statements support
no-change briefing detection. Content is never evaluated as code or
instructions.}
}
\value{
A \code{TempestKnowledge} value for \code{\link[=tempest_run]{tempest_run()}} or \code{\link[=tempest_session]{tempest_session()}}.
The run retains the exact artifact selection. Its
computed input digest binds the input description; it does not authenticate
a host, prove factual truth, or represent a native acceptance event.
}
\description{
The host resolves immutable content and checks current permission and
consultation eligibility before calling this constructor. Tempest verifies
exact selection coverage, content digests and declared dependencies. It does
not open storage, infer approval, or grant execution authority.
For Graft decisions use \code{\link[=tempest_reuse_artifact_research]{tempest_reuse_artifact_research()}}; reconstructing
detached input does not recreate its required current-admission callback.
}
\seealso{
\code{\link[=tempest_reuse_artifact_research]{tempest_reuse_artifact_research()}} for current host admission.
}
