% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/program-set.R
\name{tempest_compile_programs}
\alias{tempest_compile_programs}
\title{Compile programs in a Tempest program set}
\usage{
tempest_compile_programs(
  program_set,
  trainsets,
  teleprompters,
  path,
  valsets = NULL,
  registry = list(),
  .llm = NULL,
  compile_args = NULL
)
}
\arguments{
\item{program_set}{A \code{TempestProgramSet}.}

\item{trainsets}{Named list of training data by stage. Its names select the
stages to compile.}

\item{teleprompters}{One dsprrr Teleprompter used for every selected stage,
or an exact named list of Teleprompters.}

\item{path}{New directory for the compiled ProgramSet bundle.}

\item{valsets}{Optional exact named list of validation data for the selected
stages. Omitted stages receive \code{NULL} only when the whole argument is
omitted.}

\item{registry}{Named runtime-binding registry passed to dsprrr artifact
operations. It is not persisted.}

\item{.llm}{Optional language-model runtime passed to dsprrr compilation.}

\item{compile_args}{Optional exact named list of argument lists passed to
\code{dsprrr::compile_module()} for each selected stage.}
}
\value{
A new verified, file-backed \code{TempestProgramSet}.
}
\description{
Compiles the selected stages through \code{\link[dsprrr:compile_module]{dsprrr::compile_module()}} and only
publishes a new complete ProgramSet after every requested compilation and
artifact verification succeeds. Compilation errors are never replaced with
the original uncompiled program. Every selected baseline Module is copied
with cleared execution state and identity-checked before compilation. When
compilation changes a program artifact, the candidate drops that stage's old
governed-procedure reference.
}
