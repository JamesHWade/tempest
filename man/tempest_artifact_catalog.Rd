% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact-catalog.R
\name{tempest_artifact_catalog}
\alias{tempest_artifact_catalog}
\title{Create a typed Tempest artifact catalog}
\usage{
tempest_artifact_catalog(
  store = NULL,
  artifacts = list(),
  deliverables = list()
)
}
\arguments{
\item{store}{Optional host artifact-store adapter. Catalog writes are
persisted through the adapter before becoming visible in memory.}

\item{artifacts}{Optional initial list of typed artifacts.}

\item{deliverables}{Deliverable specifications referenced by \code{artifacts}.}
}
\value{
A \code{TempestArtifactCatalog} with \code{add()}, \code{get()}, \code{has()},
\code{version()}, \code{list()}, deliverable-registration, and \code{snapshot()} methods.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This experimental API is frozen and scheduled for removal in Tempest 0.2.0.
No compatibility shim is planned; see
\link{tempest-generic-kernel-retirement}.

The catalog owns typed artifacts for one workflow run. It supports
metadata-only listing so host applications do not need to load large
artifact content to render an output index.
}
\examples{
spec <- tempest_deliverable_spec(
  "brief",
  title = "Brief",
  purpose = "Summarize the result",
  instructions = "Be concise.",
  generator_id = "tempest.generator.markdown_report",
  renderer_ids = "tempest.renderer.markdown"
)
catalog <- tempest_artifact_catalog(deliverables = list(spec))
artifact <- tempest_artifact(spec, content = "# Brief")
catalog$add(artifact)
catalog$list()
}
