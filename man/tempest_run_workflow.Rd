% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempest-run.R
\name{tempest_run_workflow}
\alias{tempest_run_workflow}
\title{Execute an application-neutral Tempest workflow}
\usage{
tempest_run_workflow(
  objective,
  workflow,
  runtime,
  experts = list(),
  connection_permissions = list(),
  deliverables = list(),
  artifact_catalog = NULL,
  source_store = NULL,
  runtime_context = list(),
  policy_adapter = NULL,
  run_id = NULL,
  progress = NULL
)
}
\arguments{
\item{objective}{A \code{\link[=tempest_objective]{tempest_objective()}}.}

\item{workflow}{A \code{\link[=tempest_workflow_spec]{tempest_workflow_spec()}}.}

\item{runtime}{A \code{\link[=tempest_runtime]{tempest_runtime()}}, operation registry, or runtime list
containing an operation registry.}

\item{experts}{Exact pool of selected \code{\link[=tempest_expert]{tempest_expert()}} profiles.}

\item{connection_permissions}{Named list mapping expert or model-role ids to
opaque connection ids allowed for this run. Step-scoped capabilities use
the union of assigned expert-id permissions and their one shared model-role
permission.}

\item{deliverables}{Deliverable specifications available to the run.}

\item{artifact_catalog}{Optional typed artifact catalog.}

\item{source_store}{Optional \code{SourceStore} evidence ledger.}

\item{runtime_context}{Process-local named services, such as a retriever or
expert-session manager, made available to operations and capability
factories. Approved-output exporters also receive these services as their
runtime. Runtime context is never serialized.}

\item{policy_adapter}{Optional policy function or object with \code{evaluate()}.}

\item{run_id}{Optional stable run identifier.}

\item{progress}{Optional generic event callback.}
}
\value{
A mutable \code{TempestRun}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This experimental API is frozen and scheduled for removal in Tempest 0.2.0.
No compatibility shim is planned; see
\link{tempest-generic-kernel-retirement}.

Construction preflights every step operation before execution. The function
returns a mutable run in a terminal state or in nonblocking
\code{awaiting_approval} state. If execution fails after construction, the
classed error contains the inspectable failed run in \code{condition$run} and its
identity in \code{condition$run_id}. Construction and preflight errors occur
before a run exists.
}
