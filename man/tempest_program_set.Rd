% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/program-set.R
\name{tempest_program_set}
\alias{tempest_program_set}
\title{Create a validated Tempest program set}
\usage{
tempest_program_set(
  programs = NULL,
  path = NULL,
  contract_versions = 1L,
  evaluators = NULL,
  governed_procedure_refs = list(),
  registry = list()
)
}
\arguments{
\item{programs}{\code{NULL} for the exact package builtin programs, or an exact
named list of ten dsprrr Module objects.}

\item{path}{Optional directory in which to create a file-backed ProgramSet.
It must not already exist. Custom \code{programs} require this argument.}

\item{contract_versions}{Contract version \code{1}, or an exact named integer
vector containing version \code{1} for every stage.}

\item{evaluators}{\code{NULL} for Tempest's named stage output-contract
evaluators, or an exact named list whose records contain \code{evaluator_id}
and \code{evaluator_version}. These identify how stage output is judged and are
distinct from an optimization teleprompter or metric.}

\item{governed_procedure_refs}{Optional named list of typed
\code{tempest_governed_procedure_ref()} values by stage.}

\item{registry}{Named runtime-binding registry passed to dsprrr artifact
operations. It is never stored in ProgramSet metadata.}
}
\value{
A validated \code{TempestProgramSet} S7 object.
}
\description{
\code{tempest_program_set()} resolves the exact ten dsprrr programs used by the
STORM and Co-STORM product stages. With \code{programs = NULL} and \code{path = NULL},
it creates the package's addressable builtin programs without writing files.
Custom programs require \code{path} and are persisted immediately as verified
dsprrr program artifacts.
}
\details{
The returned live value retains executable modules. Its manifest projection
contains only portable identifiers, evaluator metadata, governed-procedure
references, and builtin or bundle-relative artifact references.

Resume compares program identity independently of physical location, so a
relocated verified bundle is accepted. A custom-program run must resume with
a ProgramSet carrying the same identities. Omitting \code{program_set} at a run
boundary resolves the package's current builtins and can resume only when
those builtin artifact IDs match the persisted run.
}
\keyword{internal}
