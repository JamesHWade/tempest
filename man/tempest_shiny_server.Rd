% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny-adapter.R
\name{tempest_shiny_server}
\alias{tempest_shiny_server}
\title{Run embedded Tempest Shiny panels}
\usage{
tempest_shiny_server(
  id,
  config = tempest_config(),
  store = NULL,
  panels = c("chat", "sources", "facts", "mindmap", "transcript", "report"),
  experts = NULL,
  session_id = NULL,
  program_set = NULL,
  knowledge_view = NULL
)
}
\arguments{
\item{id}{Shiny module id.}

\item{config}{A \code{TempestConfig}, a reactive returning one, a function
returning one, or \code{NULL} to use the bundled config module defaults.}

\item{store}{Optional store from \code{\link[=tempest_shiny_store]{tempest_shiny_store()}}. If \code{NULL}, a new
store is created.}

\item{panels}{Character vector matching the UI panels.}

\item{experts}{Optional expert profiles passed to \code{\link[=tempest_session]{tempest_session()}} for
new Co-STORM sessions. May be a value, function, or reactive.}

\item{session_id}{Optional stable session id passed to \code{\link[=tempest_session]{tempest_session()}}
for new Co-STORM sessions. May be a value, function, or reactive.}

\item{program_set}{Optional \link{TempestProgramSet} used for new and restored
Co-STORM sessions. May be a value, function, or reactive. \code{NULL} uses the
builtin set.}

\item{knowledge_view}{Optional immutable Graft view used with
\code{program_set}. May be a value, function, or reactive. A governed ProgramSet
requires its exact pinned view before any provider call; the view remains
process-local and is never serialized.}
}
\value{
A list with the shared \code{store}; reactive \code{costorm_session},
\code{costorm_events}, \code{costorm_evidence}, \code{storm_events}, \code{report_md},
\code{report_workspace}, and \code{report_topic} accessors; a monotonic
\code{report_navigation_event} counter; and a \code{touch_costorm_session()} control.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{tempest_shiny_server()} pairs with \code{\link[=tempest_shiny_ui]{tempest_shiny_ui()}} and lets a host app
provide a \link{TempestConfig}, optional expert profiles, a stable session id, and
an optional shared store. The returned handle exposes only product session,
event, evidence, and report state.

Progress, persistence, and successful publication use polite atomic status
regions in the bundled UI. Validation, cancellation, and publication
failures use alerts and never convert a rejected product into report-ready
state.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  tempest_shiny_server("research", config = tempest_config())
}
}
}
