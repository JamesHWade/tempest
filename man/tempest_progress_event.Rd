% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress-events.R
\name{tempest_progress_event}
\alias{tempest_progress_event}
\title{Create a Tempest progress event}
\usage{
tempest_progress_event(
  run_id,
  workflow,
  event_type,
  status,
  stage = NA_character_,
  step = NA_character_,
  message = NA_character_,
  payload = list(),
  parent_event_id = NA_character_,
  correlation_id = NA_character_,
  event_id = NULL,
  timestamp = NULL
)
}
\arguments{
\item{run_id}{Stable run or session identifier.}

\item{workflow}{Workflow kind, one of \code{"storm"} or \code{"costorm"}.}

\item{event_type}{Event family, one of \code{"workflow"}, \code{"stage"}, \code{"step"},
\code{"expert"}, \code{"tool"}, \code{"artifact"}, or \code{"cancellation"}.}

\item{status}{Event status, one of \code{"pending"}, \code{"started"}, \code{"running"},
\code{"succeeded"}, \code{"failed"}, \code{"cancelled"}, \code{"skipped"}, or \code{"available"}.}

\item{stage}{Optional workflow stage name.}

\item{step}{Optional stage-local step name.}

\item{message}{Optional short human-readable progress message.}

\item{payload}{Optional list of structured progress metadata.}

\item{parent_event_id}{Optional parent event identifier.}

\item{correlation_id}{Optional identifier shared by related events.}

\item{event_id}{Optional stable event identifier. Defaults to a generated id.}

\item{timestamp}{Optional event timestamp. Defaults to the current UTC time.}
}
\value{
A \code{tempest_progress_event} S7 object.
}
\description{
\code{tempest_progress_event()} creates a small host-neutral event record for
STORM and Co-STORM workflow progress. Events are designed for package code,
host apps, tests, and future telemetry adapters that need lifecycle,
stage, step, expert/tool, cancellation, and artifact status without
importing Shiny.
}
\details{
Core fields are validated at construction. Payloads should stay small and
privacy-aware: use them for progress metadata and references, not full raw
expert answers or source bodies by default.
}
\examples{
event <- tempest_progress_event(
  run_id = "run-1",
  workflow = "storm",
  event_type = "stage",
  status = "started",
  stage = "research"
)
event@status
}
