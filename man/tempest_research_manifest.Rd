% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research-manifest.R
\name{tempest_research_manifest}
\alias{tempest_research_manifest}
\title{Create a Tempest research manifest}
\usage{
tempest_research_manifest(
  research_run_id,
  mode = c("storm", "costorm"),
  config = NULL,
  config_digest = NULL,
  programs = list(),
  knowledge_snapshot = list(),
  runtime = list(deputy_session_ids = character(), deputy_run_ids = character()),
  traces = list(),
  deliverables = list(),
  status = "running"
)
}
\arguments{
\item{research_run_id}{Stable research-run identifier.}

\item{mode}{Product mode, either \code{"storm"} or \code{"costorm"}.}

\item{config}{A \code{TempestConfig} used to compute \code{config_digest}, or \code{NULL}
when restoring an existing digest.}

\item{config_digest}{Existing SHA-256 configuration identity. Supply this
only when \code{config} is unavailable during restoration.}

\item{programs}{Named references to exact scientific programs.}

\item{knowledge_snapshot}{Reference to a pinned accepted-knowledge snapshot.}

\item{runtime}{Opaque Deputy session and run references.}

\item{traces}{References to Deputy or dsprrr traces.}

\item{deliverables}{References to product deliverables.}

\item{status}{Run status: \code{"running"}, \code{"succeeded"}, \code{"failed"}, or
\code{"cancelled"}.}
}
\value{
A \code{TempestResearchManifest} S7 object.
}
\description{
\code{tempest_research_manifest()} records only durable identities and references
for one STORM or Co-STORM run. Supply a \code{\link[=tempest_config]{tempest_config()}} object to compute
its behavior-relevant digest. \code{config_digest} is intended for restoration of
an existing record.
}
\details{
Reference fields accept only canonical JSON-compatible plain values. They
cannot contain credentials, chats, functions, environments, connections,
S7 or R6 objects, external pointers, or missing and non-finite values.
}
\keyword{internal}
