% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{tempest_connection_provider}
\alias{tempest_connection_provider}
\title{Create a Tempest runtime connection provider}
\usage{
tempest_connection_provider(connections = list(), bindings = list())
}
\arguments{
\item{connections}{Optional list of serializable connection references.}

\item{bindings}{Named runtime factories or pre-built clients. A factory is
called with \code{connection_ref} and \code{context}. Binding names must match the
stable connection identifiers in \code{connections}.}
}
\value{
A mutable provider with \code{register()}, \code{has()}, \code{list()},
\code{preflight()}, and \code{resolve()} methods. Listings never expose bindings.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This experimental API is frozen and scheduled for removal in Tempest 0.2.0.
No compatibility shim is planned; see
\link{tempest-generic-kernel-retirement}.

A connection provider keeps authenticated clients and factories outside
durable \code{\link[=tempest_connection_ref]{tempest_connection_ref()}} records. Resolution requires an explicit
allow-list, so a workflow cannot acquire connections that were not granted
for its current execution context.
}
\examples{
reference <- tempest_connection_ref(
  "documents",
  provider_id = "host",
  connection_type = "search",
  title = "Documents",
  description = "Approved document index"
)
provider <- tempest_connection_provider(
  list(reference),
  bindings = list(documents = list(endpoint = "local"))
)
provider$resolve("documents", allowed_ref_ids = "documents")
}
