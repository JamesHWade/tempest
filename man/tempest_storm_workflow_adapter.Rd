% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builtin-workflows.R
\name{tempest_storm_workflow_adapter}
\alias{tempest_storm_workflow_adapter}
\title{Create an adapter for the built-in STORM workflow}
\usage{
tempest_storm_workflow_adapter(
  config = tempest_config(),
  retriever = NULL,
  n_experts = 3,
  runtime_factory = function() tempest_runtime(),
  stage_progress = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{config}{A \code{TempestConfig}.}

\item{retriever}{Optional retriever whose workspace must be the run's
\code{source_store}.}

\item{n_experts}{Number of experts to generate when the run starts without
an explicit expert pool.}

\item{runtime_factory}{Existing STORM runtime factory.}

\item{stage_progress}{Optional legacy STORM progress callback.}

\item{verbose}{Whether existing STORM stages print progress.}

\item{...}{Additional named \code{\link[=tempest_run]{tempest_run()}} arguments such as
\code{research_strategy} or \code{max_rounds}.}
}
\value{
A process-local workflow adapter function.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This experimental API is frozen and scheduled for removal in Tempest 0.2.0.
No compatibility shim is planned; see
\link{tempest-generic-kernel-retirement}.

The adapter executes one existing \code{\link[=tempest_run]{tempest_run()}} stage at a time against
the generic run's shared evidence store and artifact catalog. Runtime and
connection permissions always come from the owning \code{TempestRun}.
}
