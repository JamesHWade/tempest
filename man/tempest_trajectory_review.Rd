% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory-review.R
\name{tempest_trajectory_review}
\alias{tempest_trajectory_review}
\title{A bounded review of one completed Tempest product}
\usage{
tempest_trajectory_review(
  research,
  promotion_bundle = NULL,
  store = NULL,
  selection = NULL,
  stream = NULL,
  decision = NULL
)
}
\arguments{
\item{research}{One exact completed result returned by \code{\link[=tempest_run]{tempest_run()}} or a
succeeded, quiescent \code{TempestSession} returned by \code{\link[=tempest_session]{tempest_session()}}.}

\item{promotion_bundle}{Optional exact bundle returned by
\code{\link[=tempest_promotion_bundle]{tempest_promotion_bundle()}}.}

\item{store}{Artifact store used to verify an output publication. Required
with \code{selection}. The store is never retained in the review.}

\item{selection}{Exact output selection returned by
\code{\link[=tempest_publish_artifact_research]{tempest_publish_artifact_research()}}. The retained bundle and report must
match this completed research product.}

\item{stream}{Decision stream containing \code{decision}. Supply both together.}

\item{decision}{Exact historical accept decision digest. Omit to inspect a
publication without asserting acceptance. No latest-decision lookup or
current admission check is performed.}
}
\value{
A validated \code{TempestTrajectoryReview} S7 value containing the closed
ten-field bounded projection. The class is internal and the review is not
persisted.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Builds a deterministic, read-only projection of the exact execution,
program, knowledge, and evidence identities retained by a completed STORM
or Co-STORM product. Correlation identifiers are grouping evidence only;
they never establish causation. The returned projection is reconstructable
in memory and is not a persistence or acceptance authority.

The value contains exactly \code{schema_version}, \code{review_id}, \code{product},
\code{stages}, \code{agent_runs}, \code{programs}, \code{knowledge}, \code{evidence}, \code{joins}, and
\code{findings}. The \code{stages} lane retains authoritative StageRecord order. The
\code{agent_runs}, \code{evidence}, \code{joins}, and \code{findings} lanes are canonical sets;
input artifact identities use the same canonical envelope beneath
\code{knowledge$input_selection$records}. Every variable lane contains
exactly \code{total}, \code{retained}, \code{omitted}, \code{digest}, and \code{items}, retains at most
250 items, and binds the complete lane digest. Mutable progress events,
prompts, responses, source content, paths, credentials, capabilities, and
live objects are excluded.

Joins distinguish authority-validated bindings, exact identity, and
correlation-only grouping. A \code{correlation_id} can support only a
\code{correlated_with} relation and never claims causation or authorship. An
exact promotion bundle or verified publication adds proposed state. An exact
recorded accept decision adds historical accepted state. This remains
inspectable after withdrawal and never grants current reuse permission.
Input selection identities and their full metadata digest remain distinct
from output publication. Input \code{reported_decision} is unverified host
provenance, never authenticated acceptance; malformed metadata is omitted.
Decision actor, reason, key and other arbitrary input provenance are
excluded. Schema 3 retains artifact inputs and directly replaces native
snapshot and receipt-based reviews.
}
\examples{
\dontrun{
result <- tempest_run("Grid-scale battery recycling")
review <- tempest_trajectory_review(result)
review@product
review@stages
}
}
