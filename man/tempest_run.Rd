% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storm.R
\name{tempest_run}
\alias{tempest_run}
\title{Run the STORM pipeline}
\usage{
tempest_run(
  topic,
  config = tempest_config(),
  retriever = NULL,
  n_experts = 3,
  experts = NULL,
  runtime = tempest_runtime(),
  runtime_factory = function() tempest_runtime(),
  connection_permissions = list(),
  research_strategy = c("key_questions", "conversation"),
  max_rounds = 3,
  max_questions_per_perspective = 3,
  parallel_research = FALSE,
  parallel_writing = FALSE,
  dsprrr_modules = NULL,
  steps = c("perspectives", "research", "outline", "write", "polish"),
  output_dir = NULL,
  resume = FALSE,
  run_id = NULL,
  remove_duplicate = FALSE,
  progress = NULL,
  verbose = TRUE,
  artifact_catalog = NULL,
  workflow_run = NULL,
  .state = NULL
)
}
\arguments{
\item{topic}{Research topic or question.}

\item{config}{A \code{TempestConfig}.}

\item{retriever}{Optional \code{TempestRetriever}. If \code{NULL}, created from \code{config}.}

\item{n_experts}{Number of expert profiles to generate when \code{experts} is
\code{NULL} (default 3).}

\item{experts}{Optional list of active profiles created by
\code{\link[=tempest_expert]{tempest_expert()}}. When supplied, STORM uses this selected team and does
not generate experts.}

\item{runtime}{Frozen Tempest 0.1 \code{\link[=tempest_runtime]{tempest_runtime()}} adapter. Existing
integrations only.}

\item{runtime_factory}{Function that recreates the frozen 0.1 \code{runtime}
inside parallel workers. Existing integrations using a custom runtime with
\code{parallel_research = TRUE} must provide a matching factory.}

\item{connection_permissions}{Frozen Tempest 0.1 mapping from expert or
model-role ids to opaque connection ids allowed for this run.}

\item{research_strategy}{Either "key_questions" (default, faster) or "conversation"
(more thorough but slower). Key questions uses predefined questions; conversation
dynamically generates follow-up questions.}

\item{max_rounds}{Maximum rounds per perspective for "conversation" strategy (default 3).}

\item{max_questions_per_perspective}{Maximum questions per perspective for "key_questions"
strategy (default 3).}

\item{parallel_research}{If \code{TRUE}, run research perspectives in parallel using
the mirai package. Requires mirai to be installed. Default \code{FALSE}.}

\item{parallel_writing}{If \code{TRUE}, write report sections in parallel using
the mirai package. Failed parallel sections are retried sequentially.}

\item{dsprrr_modules}{Optional named list of dsprrr modules, typically from
\code{\link[=tempest_optimize_dsprrr_modules]{tempest_optimize_dsprrr_modules()}}. If \code{NULL}, fresh modules are created.}

\item{steps}{Character vector controlling which steps to run. Defaults to all.}

\item{output_dir}{Optional directory for persisted STORM run artifacts. When
supplied, artifacts are written under a topic-specific subdirectory.}

\item{resume}{If \code{TRUE} and \code{output_dir} contains a previous run, load saved
artifacts and skip stages recorded as complete.}

\item{run_id}{Optional run directory name. Defaults to a slug of \code{topic}.}

\item{remove_duplicate}{If \code{TRUE}, ask the polish step to remove duplicate
or highly repetitive content while preserving unique cited claims.}

\item{progress}{Optional function called with a \code{tempest_progress_event}
object as STORM workflow stages start, finish, fail, persist artifacts, or
make final artifacts available.}

\item{verbose}{If \code{TRUE}, prints progress messages.}

\item{artifact_catalog}{Frozen Tempest 0.1 shared
\code{TempestArtifactCatalog}, used by the generic STORM workflow adapter.}

\item{workflow_run}{Frozen Tempest 0.1 owning \code{TempestRun}. When supplied,
the result exposes it as \code{workflow_run}.}

\item{.state}{Internal adapter-only fixed STORM product state. This is not a
public continuation API.}
}
\value{
A list with product fields \code{title}, \code{perspectives}, \code{experts},
\code{outline}, \code{draft_md}, \code{report_md}, \code{manifest}, \code{state}, \code{workspace},
\code{retriever}, and \code{output_dir}. The compatibility field \code{store} references
the same object as \code{workspace}. Frozen 0.1 compatibility fields
\code{artifact_catalog} and \code{workflow_run} are also returned temporarily.
}
\description{
This is a scripted workflow that:
\enumerate{
\item discovers perspectives and research questions,
\item runs a multi-perspective research loop (search/fetch + expert synthesis),
\item creates an outline,
\item writes a cited report in Markdown.
}
}
\section{Frozen Tempest 0.1 seams}{


\code{runtime}, \code{runtime_factory}, \code{connection_permissions}, \code{artifact_catalog},
and \code{workflow_run} expose the frozen experimental generic kernel. They remain
only for existing Tempest 0.1 integrations and are scheduled for replacement
or removal in Tempest 0.2.0. New code should consume \code{report_md}, \code{manifest},
\code{state}, and the scientific evidence in \code{workspace}.
}

\examples{
\dontrun{
cfg <- tempest_config()
result <- tempest_run("History of jazz", config = cfg)
cat(result$report_md)
}
}
