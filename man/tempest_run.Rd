% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storm.R
\name{tempest_run}
\alias{tempest_run}
\title{Run the STORM pipeline}
\usage{
tempest_run(
  topic,
  config = tempest_config(),
  retriever = NULL,
  n_experts = 3,
  research_strategy = c("key_questions", "conversation"),
  max_rounds = 3,
  max_questions_per_perspective = 3,
  parallel_research = FALSE,
  steps = c("perspectives", "research", "outline", "write", "polish"),
  verbose = TRUE
)
}
\arguments{
\item{topic}{Research topic or question.}

\item{config}{A \code{TempestConfig}.}

\item{retriever}{Optional \code{TempestRetriever}. If \code{NULL}, created from \code{config}.}

\item{n_experts}{Number of expert personas to use (default 3).}

\item{research_strategy}{Either "key_questions" (default, faster) or "conversation"
(more thorough but slower). Key questions uses predefined questions; conversation
dynamically generates follow-up questions.}

\item{max_rounds}{Maximum rounds per perspective for "conversation" strategy (default 3).}

\item{max_questions_per_perspective}{Maximum questions per perspective for "key_questions"
strategy (default 3).}

\item{parallel_research}{If \code{TRUE}, run research perspectives in parallel using
the mirai package. Requires mirai to be installed. Default \code{FALSE}.}

\item{steps}{Character vector controlling which steps to run. Defaults to all.}

\item{verbose}{If \code{TRUE}, prints progress messages.}
}
\value{
A list with \code{title}, \code{outline}, \code{draft_md}, \code{report_md}, and \code{store}.
}
\description{
This is a scripted workflow that:
\enumerate{
\item discovers perspectives and research questions,
\item runs a multi-perspective research loop (search/fetch + expert synthesis),
\item creates an outline,
\item writes a cited report in Markdown.
}
}
