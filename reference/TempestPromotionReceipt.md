# Proof that one reviewed promotion plan was accepted

Proof that one reviewed promotion plan was accepted

## Usage

``` r
TempestPromotionReceipt(
  schema_version = integer(0),
  receipt_id = character(0),
  bundle_id = character(0),
  plan_id = character(0),
  plan_digest = character(0),
  batch_id = character(0),
  store_id = character(0),
  schema_build_digest = character(0),
  snapshot = list(),
  counts = list(),
  record_revisions = list()
)
```
