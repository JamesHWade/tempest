# Create a Fact note (atomic claim with citations)

Create a Fact note (atomic claim with citations)

## Usage

``` r
tempest_fact(
  claim,
  source_ids,
  confidence = NA_character_,
  note = NA_character_,
  tags = character()
)
```
