# Create a STORM configuration

Create a STORM configuration

## Usage

``` r
tempest_config(...)
```

## Arguments

- ...:

  Passed to `TempestConfig$new()`.

## Value

A `TempestConfig` R6 object.
