# Require a package or abort

Checks if a package is installed and aborts with a helpful message if
not.

## Usage

``` r
tempest_require(pkg, why = NULL)
```

## Arguments

- pkg:

  Package name.

- why:

  Optional reason explaining why the package is needed.
