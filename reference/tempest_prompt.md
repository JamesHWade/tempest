# Load a bundled prompt template

Load a bundled prompt template

## Usage

``` r
tempest_prompt(name)
```

## Arguments

- name:

  Prompt name (without extension), e.g. "writer_system".

## Value

A character string containing the prompt.
