# Check if provider has native web search

Check if provider has native web search

## Usage

``` r
tempest_provider_has_native_search(provider)
```

## Arguments

- provider:

  Provider name

## Value

TRUE if provider supports native web search
