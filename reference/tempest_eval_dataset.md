# Load an included evaluation dataset

Load an included evaluation dataset

## Usage

``` r
tempest_eval_dataset(name = c("qa"))
```

## Arguments

- name:

  Dataset name. Currently supports "qa".

## Value

A tibble with columns `input` and `target`.
