# Create a TempestRetriever

Create a TempestRetriever

## Usage

``` r
tempest_retriever(config = tempest_config(), store = SourceStore$new())
```

## Arguments

- config:

  A `TempestConfig`.

- store:

  A `SourceStore`.

## Value

A `TempestRetriever`.
